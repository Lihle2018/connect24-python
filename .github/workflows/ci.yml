name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # The floor and the current release. A library is installed into applications whose Python
        # version nobody consulted us about, so both ends have to stay green.
        python: ['3.9', '3.13']

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - run: pip install pytest
      - run: python -m pytest tests -q
        env:
          PYTHONPATH: src

      # The samples are part of the contract: each ships with tests that drive the real
      # SDK over a stubbed transport, and they must stay green with the SDK they sit on.
      - name: Sample tests
        run: python -m pytest examples/order_notifications -q
        env:
          PYTHONPATH: src:examples/order_notifications
